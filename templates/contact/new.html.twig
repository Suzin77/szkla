{% extends 'base.html.twig' %}

{% block title %}New Contact{% endblock %}

{% block body %}
    <h1>Create new Contact</h1>

    {{ include('contact/_form.html.twig') }}

    <a class="btn btn-sm btn-primary mr-2" href="{{ path('contact_index') }}">back to list</a>
{% endblock %}
